@page "/"
@page "/chat"
@page "/chat/session/{sessionId:int}"

<PageTitle>ChatPro-Max</PageTitle>

<DialogueArea sessionId="@sessionId"/>

@code{
    [Parameter]
    public int sessionId { get; set; }
}